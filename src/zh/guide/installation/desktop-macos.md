# <svg-icon name="apple" size="36px" /> 在 macOS 上安装

## 系统要求
- 系统版本：macOS 12.0+
- 内存：8GB
- CPU：2核

## 安装步骤

1. 根据你的芯片型号（Apple Silicon[M1/M2/M3/M4] 或 Intel）下载 `.dmg` 安装文件；
2. 打开 `.dmg` 文件，将 **PileaX** 拖入 `应用程序` 文件夹；
3. 在 `应用程序` 中打开 PileaX。